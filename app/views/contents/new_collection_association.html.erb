<h1>New content collection association</h1>

<%= form_for(@content) do |f| %>

  <div class="field">
    <%= f.hidden_field :content_id %>
  </div>

  <div class="field">
  	<%= f.label :collection %><br />
  	<%= f.select :collection_id Collection.all

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>

<%= link_to 'Back', contents_path %>
